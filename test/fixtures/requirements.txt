# Pinned: the fixtures are committed and CI checks that regenerating them changes nothing, so a
# different ezdxf version would fail that check rather than quietly produce different files.
ezdxf==1.4.4
